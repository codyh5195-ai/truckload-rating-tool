# Copy to .env — Vite exposes only variables prefixed with VITE_ to the browser.
# Do NOT put secret keys here.

# Base URL for the backend API (leave blank to use Vite's proxy in development)
VITE_API_BASE_URL=
